const SET_VALUE_CONFIG = {
  shouldValidate: true,
  shouldDirty: true,
  shouldTouch: true,
};

const HTTP_METHODS = {
  HEAD: "HEAD",
  GET: "GET",
  PUT: "PUT",
  PATCH: "PATCH",
  POST: "POST",
  DELETE: "DELETE",
};

const PLACEHOLDER = {
  DATA: "--",
  DATE: "----/----/----",
  MONEY: "₹__",
};

export { HTTP_METHODS };

const CONSTANTS = {
  PLACEHOLDER,
};

export default CONSTANTS;
